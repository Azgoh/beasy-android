<?xml version="1.0" encoding="utf-8"?>
<!--
    btn_primary.xml — The dark blue rounded Register button background.
    Using a selector means Android can change the look when pressed.

    WHERE: Place this in app/src/main/res/drawable/btn_primary.xml
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- When the button is being pressed (darker blue) -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#142D72" />
            <corners android:radius="10dp" />
        </shape>
    </item>

    <!-- When button is disabled (greyed out) -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="#9EB0D8" />
            <corners android:radius="10dp" />
        </shape>
    </item>

    <!-- Default state (normal dark blue) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/primary_blue" />
            <corners android:radius="10dp" />
        </shape>
    </item>

</selector>